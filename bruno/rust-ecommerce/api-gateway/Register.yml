info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/auth/register
  body:
    type: json
    data: |-
      {
        "email": "petch1@gmail.com",
        "password": "123456789"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
