info:
  name: Get Product list
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8080/api/products?page=1&per_page=6
  headers:
    - name: Authorization
      value: Bearer {{access_token}}
  params:
    - name: page
      value: "1"
      type: query
    - name: per_page
      value: "6"
      type: query
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
